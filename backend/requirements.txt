# Minimal production dependencies — only what backend/server.py imports.
# (The original Emergent environment bundled ~120 packages incl. a private
#  'emergentintegrations' package that isn't on public PyPI and breaks pip.)
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0          # required for mongodb+srv:// (Atlas) connection strings
httpx==0.28.1
pydantic==2.12.5
python-dotenv==1.2.1
